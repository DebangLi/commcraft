from setuptools import setup

setup(name='commcraft',
      version='0.0.1',
      description='a common implation for starcraft in python',
      license='MIT License',
      install_requires=['gym>=0.2.3'])
